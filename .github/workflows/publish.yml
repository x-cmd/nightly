name: Package and Publish
on:
  # push:
  workflow_dispatch:
  schedule:
    - cron: '30 11 * * *'

jobs:
  publisher:
    runs-on: ubuntu-latest
    env:
      ssh_key: ${{ secrets.SSH_PRIVATE_KEY }}
      git_user: agent-zero
      git_email: 0@x-cmd.com
    steps:
      - name: main
        uses: x-cmd/action@main
        with:
          code: CONCURRENCY=8 x ws pub || true
