# shellcheck shell=sh disable=SC3043

___x_cmd_hub_msg_telegram(){
    local msg="${1}"
    local res
    if ! res="$(___x_cmd_hub_client_send_telegram_msg "$msg")" ; then
        hub:error "Failed to send msg, res:$res, msg:$msg"
        return 1
    fi
    hub:info "response: ${res}"
}